import React from 'react';

function FunctionalComponentSample() {
  const greeting = 'Hello, I am a functional component';

  // Returns JSX (a React element) like so:
  return <p>{greeting}</p>;
}

export default FunctionalComponentSample;
